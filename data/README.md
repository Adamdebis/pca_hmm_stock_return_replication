# Data folder

The default demonstration generates a deterministic synthetic return panel and
writes it to `results/synthetic_returns.csv`.

For an empirical run, place a dated CSV here with one date/index column and
one weekly-return column per asset. Do not commit large raw downloads or
credentials. Record the source, download date, universe definition, and any
missing-value treatment in a short provenance note.
